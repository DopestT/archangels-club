# Server
PORT=4000
JWT_SECRET=change_this_to_a_strong_random_secret
CLIENT_URL=http://localhost:3000

# Client (prefix with VITE_ to expose to browser)
# No secrets here — API calls go through /api proxy
